<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".setpass2"></activity>
        <activity android:name=".setpass1" />
        <activity android:name=".registeredind" />
        <activity android:name=".registeredhos" />
        <activity android:name=".login" />
        <activity android:name=".requests2" />
        <activity android:name=".requests" />
        <activity android:name=".formfilling2" />
        <activity android:name=".formfilling1" />
        <activity android:name=".choice" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>